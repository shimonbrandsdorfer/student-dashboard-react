// import { Router } from "express";
// import {getById} from "../services/teachers.js";
// const router = Router();

// router.get('/:id', async (req, res) => {
//     const teacherId = parseInt(req.params.id, 10);
//     const teacher = getById(teacherId);
//     res.send(teacher);
// });

// export default router;